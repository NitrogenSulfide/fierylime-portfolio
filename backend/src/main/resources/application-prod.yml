spring:
  datasource:
    url: jdbc:${DATABASE_URL}

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true
    validate-on-migrate: true

logging:
  level:
    org.flywaydb: INFO

server:
  address: 0.0.0.0
  port: ${PORT:8080}
